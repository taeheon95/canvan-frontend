import React from "react";
import styled from "styled-components";

interface PropType {}

const AuthTemplateBlock = styled.div`
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  right: 0;
  background: 0;
`;

function LoginPresenter(props: PropType) {
  return <div></div>;
}

export default LoginPresenter;
